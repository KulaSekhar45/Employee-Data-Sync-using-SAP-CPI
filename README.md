# Employee-Data-Sync-using-SAP-CPI
This project demonstrates a real-time integration scenario built using SAP Cloud Platform Integration (CPI).  It fetches employee data from a public API, processes it, filters records, transforms the structure, and sends it to a target system.
